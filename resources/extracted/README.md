# resources/extracted

Plain-text extractions of the Tibetan, kept so they are not lost to a session. Generated files — do not hand-edit; regenerate with the commands in `tools/compare_source.py`.

| file | what |
|---|---|
| `source-epub-2561-text.md` | the newer source EPUB (`../བཀའ་རྙིང་ཆོས་སྤྱོད་ཞལ་འདོན།.epub`, Pages export dated 2561-06-27), pure Tibetan, one `#` per chapter, yigchung marked `«…»` |
| `current-src-text.md` | the same for our `src/`, navigation stripped, one `#` per heading |
| `*.json` | the same with run styles (n normal, y yigchung, r our written-out refrain), input to the comparison |
| `comparison-2026-09-12.md` | TOC alignment, passages present in only one book, and every small textual variant — with a findings summary at the top |

The source EPUB is text only: no jump links, no page numbers, no editorial apparatus. Use it for the Tibetan, not the structure.
